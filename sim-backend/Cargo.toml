[package]
name = "sim-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
sim-core = { path = "../sim-core" }
wgpu = "0.20"
bytemuck = { version = "1.15", features = ["derive"] }
pollster = "0.3"
